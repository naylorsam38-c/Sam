"""Aura — self-hosted live interactive avatar presentation service.

The avatar is a *presentation service*: it receives response text (+ optional
performance instructions) from an external brain (Command Desk), and turns it
into synchronised speech + facial animation streamed over WebRTC. It never
contains or replaces the brain.
"""
__version__ = "1.0.0"

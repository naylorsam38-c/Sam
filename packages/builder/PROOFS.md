# Engine proofs -- actual captured output

Generated by `run_all_proofs.py`. Every entry below is the real return value of that engine's own `prove()`, run against a real system, just now -- not a hand-written claim.


## audit_trail

**Real system**: sqlite3 (:memory:, a real database connection)

**Steps**:
- create row + log
- update row + log
- delete row + log
- query history back

**Observed** (actual captured output):
```json
[
  {
    "action": "create",
    "before": null,
    "after": {
      "title": "Write report",
      "stage": "To do"
    },
    "at": 1788531896.7626398
  },
  {
    "action": "edit",
    "before": {
      "title": "Write report",
      "stage": "To do"
    },
    "after": {
      "title": "Write report",
      "stage": "In progress"
    },
    "at": 1788531896.762834
  },
  {
    "action": "delete",
    "before": {
      "title": "Write report",
      "stage": "In progress"
    },
    "after": null,
    "at": 1788531896.762897
  }
]
```


## stage_history

**Real system**: sqlite3 (:memory:, a real database connection)

**Steps**:
- record 3 real deals' real stage transitions at real timestamps
- query entered_at('Won') for Deal 1
- compute real win rate over Won/Lost

**Observed** (actual captured output):
```json
{
  "entered_at_won_D1": 1002.0,
  "win_rate": 0.5
}
```


## stage_conditional_requiredness

**Real system**: pure function, exercised for real (no db needed)

**Steps**:
- reject a real Lost deal missing Lost reason
- accept the same deal once Lost reason is set
- accept a Won deal with no rule at all

**Observed** (actual captured output):
```json
{
  "rejection_message": "stage 'Lost' requires ['Lost reason'], got {'Title': 'Acme deal', 'Lost reason': None}"
}
```


## record_cloning

**Real system**: sqlite3 (:memory:, a real database connection)

**Steps**:
- insert a real Task row
- clone it with overrides+suffix
- query back both real rows

**Observed** (actual captured output):
```json
{
  "original": [
    "Write report",
    "In progress"
  ],
  "clone": [
    "Write report (copy)",
    "To do"
  ],
  "new_id_distinct": true
}
```


## scheduling_availability

**Real system**: sqlite3 (:memory:, a real database connection)

**Steps**:
- insert one real 30-minute appointment for Staff A at t=1000 (occupies [1000,2800))
- check an overlapping proposed slot (t=1010, same staff) -> conflict
- check a clear slot (t=2800, right after it ends) -> no conflict
- check the same overlapping time for a different staff member -> no conflict

**Observed** (actual captured output):
```json
{
  "overlapping": [
    [
      "A-1",
      1000.0
    ]
  ],
  "clear": [],
  "different_staff": []
}
```


## scheduled_jobs

**Real system**: threading.Timer (a real background thread) + sqlite3

**Steps**:
- schedule a real 0.3s job on an unpaid appointment, real-wait 0.6s -> really expires
- schedule a real job on an appointment whose deposit is paid before it fires -> does not expire
- schedule a real job then cancel() it before it fires -> never mutates the row

**Observed** (actual captured output):
```json
{
  "before": "Booked",
  "after": "Cancelled",
  "paid_before_fire_stage": "Booked",
  "cancelled_job_stage": "Booked"
}
```


## stock_ledger

**Real system**: sqlite3 (:memory:, a real database connection)

**Steps**:
- real Product starts stock=10 reorder_point=5
- real Sales order line qty=6 reaches Shipped -> subtract 6
- real Purchase order line qty=20 reaches Received -> add 20

**Observed** (actual captured output):
```json
{
  "stock_after_ship": 4,
  "reorder_needed_after_ship": true,
  "stock_after_receive": 24,
  "reorder_needed_after_receive": false
}
```


## ledger_balancing

**Real system**: sqlite3 (:memory:, a real database connection)

**Steps**:
- real Invoice total=100.00
- apply real Payment of 40 -> not Paid
- apply real Payment of 60 -> Paid (40+60>=100)

**Observed** (actual captured output):
```json
{
  "paid_after_first_payment": false,
  "paid_after_second_payment": true,
  "total_applied": 100.0
}
```


## search_fts

**Real system**: sqlite3 FTS5 (:memory:, a real virtual table)

**Steps**:
- index 3 real task rows
- real MATCH query for 'invoice'
- real MATCH query for a word in none

**Observed** (actual captured output):
```json
{
  "invoice_hits": [
    "T-2"
  ],
  "nothing_hits": []
}
```


## import_export

**Real system**: a real CSV file on disk + sqlite3 (:memory:)

**Steps**:
- insert 3 real rows
- export to a real temp CSV file
- delete all rows
- import back from that same real file
- compare round-trip

**Observed** (actual captured output):
```json
{
  "exported_count": 3,
  "imported_count": 3,
  "rows_back": [
    [
      "C-1",
      "Ada Lovelace",
      "ada@example.com"
    ],
    [
      "C-2",
      "Grace Hopper",
      "grace@example.com"
    ],
    [
      "C-3",
      "Alan Turing",
      "alan@example.com"
    ]
  ],
  "csv_bytes": 115
}
```


## file_conversion

**Real system**: real files on disk (csv + json)

**Steps**:
- write a real CSV file
- convert to real JSON file
- convert back to real CSV
- compare round-trip

**Observed** (actual captured output):
```json
{
  "csv_to_json_rows": 2,
  "json_to_csv_rows": 2,
  "round_trip_equal": true
}
```


## email_parsing

**Real system**: Python stdlib email module, real RFC5322/MIME bytes

**Steps**:
- build a real MIME multipart message with a real attachment
- serialise it to real bytes
- parse those real bytes back
- check every field round-trips

**Observed** (actual captured output):
```json
{
  "subject": "Your invoice INV-0042",
  "from": "billing@example.com",
  "to": "customer@example.com",
  "body": "Please find your invoice attached. Thanks for your business.",
  "attachments": [
    {
      "filename": "invoice.pdf",
      "content_type": "application/pdf",
      "size": 43
    }
  ]
}
```


## bank_feed_ofx

**Real system**: xml.etree.ElementTree over a real, spec-shaped OFX 2.0 file

**Steps**:
- parse a real OFX 2.0 XML bank statement
- check both real transactions extracted

**Observed** (actual captured output):
```json
[
  {
    "fitid": "2026081200001",
    "type": "CREDIT",
    "date": "20260812000000",
    "amount": 1250.0,
    "memo": "INV-0042 PAYMENT RECEIVED"
  },
  {
    "fitid": "2026081500002",
    "type": "DEBIT",
    "date": "20260815000000",
    "amount": -89.5,
    "memo": "OFFICE SUPPLIES CO"
  }
]
```


## calendar_ics

**Real system**: hand-written RFC 5545 text, generated and parsed for real

**Steps**:
- generate real iCalendar text for 2 real events (with commas/semicolons in the summary)
- parse that real text back
- check exact round-trip

**Observed** (actual captured output):
```json
{
  "generated_bytes": 355,
  "parsed_events": [
    {
      "uid": "evt-1@example.com",
      "dtstart": "20260910T140000Z",
      "dtend": "20260910T143000Z",
      "summary": "Client call, re: renewal"
    },
    {
      "uid": "evt-2@example.com",
      "dtstart": "20260911T090000Z",
      "dtend": "20260911T100000Z",
      "summary": "Team sync; sprint planning"
    }
  ]
}
```


## document_signing

**Real system**: hmac + hashlib (stdlib), real byte-level document data

**Steps**:
- sign a real document with a real secret
- verify genuine -> True
- mutate one real byte -> verify -> False
- verify with the wrong secret -> False

**Observed** (actual captured output):
```json
{
  "signature": "545c54f1eb57e26d1153ab73c29e8c52af0b1755c34bb0c5d67cf2fa559bee5c",
  "genuine": true,
  "tampered_ok": false,
  "wrong_secret_ok": false
}
```


## document_generation

**Real system**: a real PDF file on disk, read back by an independent stdlib parser

**Steps**:
- render a real minimal PDF with a title + 3 lines (one with real parentheses)
- check real %PDF header / %%EOF trailer bytes
- parse it back with a SEPARATE, independent stdlib parser (not render_pdf's own code)
- check every line of text recovered, in order, exactly
- also render the same content as real HTML

**Observed** (actual captured output):
```json
{
  "pdf_bytes": 752,
  "recovered_text": [
    "Invoice INV-0042",
    "Bill to: Acme (Pty) Ltd",
    "Total due: 1250.00 AUD",
    "Terms: net 30 days"
  ]
}
```


## pdf_form_filling

**Real system**: a real PDF file on disk, read back by an independent stdlib parser

**Steps**:
- render a real PDF with 2 real blank AcroForm fields
- read both real fields back (both blank)
- fill one real field with a real value, re-render
- read both real fields back again -- only the filled one changed

**Observed** (actual captured output):
```json
{
  "before": [
    {
      "name": "customer_name",
      "value": ""
    },
    {
      "name": "invoice_total",
      "value": ""
    }
  ],
  "after": [
    {
      "name": "customer_name",
      "value": "Acme (Pty) Ltd"
    },
    {
      "name": "invoice_total",
      "value": ""
    }
  ]
}
```

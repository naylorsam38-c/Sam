{
  "name": "sam-standalone",
  "version": "1.0.0",
  "description": "Chat, the complex-request lifecycle, and tasks",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test-lifecycle.js && node test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
